package org.example.model.scenario.characters.side

import org.example.model.Character
import org.example.model.scenario.Rooms

/**
 * (Gouvernante): Sie kümmert sich um das Personal des Schlosses und versucht, die Ordnung aufrechtzuerhalten, auch wenn die Ereignisse eskalieren.
 */
class KarinSvensson : Character("Karin Svensson", Rooms.hall)