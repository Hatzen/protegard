package org.example.model.scenario.characters.side.village

import org.example.model.Character
import org.example.model.scenario.Rooms

/**
 * (Dorfältester): Er lebt in einem nahegelegenen Dorf und kennt die düsteren Geschichten über das Schloss.
 */
class EinarOlsen : Character("Einar Olsen", Rooms.villageEntry)